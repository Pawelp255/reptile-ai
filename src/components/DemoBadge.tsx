/** Badge shown when app is in demo/expo mode */
export function DemoBadge() {
  return (
    <span className="inline-flex items-center px-2 py-0.5 rounded-md text-[10px] font-medium bg-muted/90 text-muted-foreground transition-opacity duration-150">
      Demo
    </span>
  );
}
